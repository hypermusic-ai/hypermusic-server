#pragma once

#include <spdlog/spdlog.h>

namespace hm
{
    class Condition
    {
        public:
            Condition() = default;
            ~Condition() = default;
            
        protected:

        private:

    };
}