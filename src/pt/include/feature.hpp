#pragma once

#include <spdlog/spdlog.h>

namespace hm
{
    class Feature
    {
        public:
            Feature() = default;
            ~Feature() = default;
            
        protected:

        private:

    };
}